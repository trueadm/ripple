component Button({$count}) {
  <button onClick={() => { $count = $count + 1; } }>{"child: "}{$count}</button>
}

export component App() {
  <div>
    let $count = 0;

    <button onClick={() => { $count = $count + 1; } }>{"parent: "}{$count}</button>
    <Button {$count} />
  </div>
}