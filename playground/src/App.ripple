import { array, createContext, getContext, setContext } from 'ripple';

const MyContext = createContext(null);

component Child() {
  const value = getContext(MyContext);

  <div>{value}</div>
}

export component App() {
  const value = getContext(MyContext);

  console.log(value);

  const items = new Array(1, 2, 3);

  setContext(MyContext, "Hello from context!");

  <Child />
}